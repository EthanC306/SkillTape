================================================================================
                        SKILLTAPE — PROJECT UPDATES LOG
================================================================================
Newest entries at the top. Copy the entry template below to add a new update.

--------------------------------------------------------------------------------
ENTRY TEMPLATE (copy/paste for new entries)
--------------------------------------------------------------------------------
Date:     YYYY-MM-DD
Commit:   <hash or "uncommitted">
Files:    <files changed>
Summary:
  - <what changed and why>


--------------------------------------------------------------------------------
A3 pilot migration: dynamic-alloc's real source + 8 new itemSchema items
--------------------------------------------------------------------------------
Date:     2026-08-01
Commit:   uncommitted
Files:    sources/cpp/dynamic-alloc.md (new, gitignored), sources/README.md
          (new), .gitignore, src/data/topics/cpp/dynamic-alloc.js
Summary:
  - Transcribed the real data into sources/cpp/dynamic-alloc.md
    with 13 anchored sections, per the CS_DRILL_BUILD_SPEC.md source-file
    format. Verbatim, not paraphrased. Gitignored — never pushed, matches the
    existing copyrighted-material policy. The PDF itself was not saved
    anywhere in the repo.
  - Added sources/README.md documenting the format (the one file in
    sources/ that IS meant to be committed). Had to fix .gitignore along the
    way: `sources/` as a bare directory ignore silently blocks re-including
    anything inside it even with `!sources/README.md` — git won't descend
    into an already-ignored directory to re-evaluate a negation. Changed to
    `sources/*` + the negation, which actually works (verified with
    `git add --dry-run`, the authoritative test).
  - Added an `items` array (8 items) to dynamic-alloc.js using the new
    itemSchema.js shape — RECALL/TRACE/ERROR/CLOZE/COMPARE/WRITE, no MCQ —
    each with real `provenance` citing an anchor in the new source file.
    origin: GENERATED (I wrote these from the source excerpts; "hand-written
    only" was dropped as a live constraint on this phase, per request — see
    ROADMAP.md A3's [UPDATED] note). verifiedByHuman is false on all 8 —
    that review pass is still owed, and there's no UI for it yet (A7).
  - Did NOT touch the existing `questions` array (13 legacy MCQs) —
    QuizView still reads it directly, so the live Quiz feature is
    unaffected. auditBank.js prefers `items` over `questions` when both are
    present, so the audit now validates the new schema for this topic.
    dynamic-alloc now audits at zero errors (bank-wide: 1230 -> 1165).

--------------------------------------------------------------------------------
ROADMAP.md: FSRS decision (D10), A0 (exam weighting), A1 (hygiene) done
--------------------------------------------------------------------------------
Date:     2026-08-01
Commit:   uncommitted
Files:    docs/ROADMAP.md, index.html, src/Shell.jsx, src/data/theme.js,
          src/data/topics/cpp/*.js (all 12), gaps/inbox.jsonl (new)
Summary:
  - docs/ROADMAP.md: added D10 — scheduler algorithm changes from Leitner to
    FSRS (via the ts-fsrs library), per request. Rewrote A4's "Scheduler"
    section, D8's diagram-weighting note, and every idea-ledger row that
    pointed at Leitner to reflect this. Also did a general accuracy pass:
    re-ran npm run audit:bank and updated stale error/warning counts, fixed a
    contradiction where D5 read as unchallenged despite Track B already having
    shipped ahead of Track A, and added a cross-reference to the newer
    docs/PRODUCTION_READINESS.md.
  - A0 (calibrate against the midterm): no graded exam was available, so this
    ran as a live diagnostic quiz + self-reported struggle areas instead of
    real point tallies (documented as a deliberate deviation in ROADMAP.md).
    Added `examWeight` to all 12 cpp topic files — 2.0 for dynamic-alloc and
    1.7 for dynamic-arrays (confirmed gaps from the quiz), 1.5 for four other
    self-reported-struggle topics, 1.0 default for the rest. discrete topics
    still have none (different course, no exam data yet). Logged two
    confirmed gaps to the new gaps/inbox.jsonl.
  - A1 (hygiene pass): theme.js's PALETTE.accent now reads
    var(--nocturne-accent) instead of restating the hex (fixes the token
    duplication CORRECTIONS.md §2.5 flagged). Shell.jsx's tab bar <nav> now
    carries className="app-chrome", so index.html's existing drill-mode CSS
    rule can actually hide it. Verified both survive `npx vite build` into
    the compiled bundle; not click-tested in a live browser (no
    browser-automation tool was available in this environment).

--------------------------------------------------------------------------------
Added flashcards
--------------------------------------------------------------------------------
Date:     2026-08-01
Commit:   <commited">
Files:    <auth.js progress.js curriculum.js>
Summary:
  - <what changed and why>
- Added src/data/topics/discrete/vocabulary.js — a new "Vocabulary" topic under CS3000, with one intro card explaining the deck, no quiz questions, and an empty flashcards array for you to fill in yourself.
- Registered it in src/data/curriculum.js.
- Fixed a latent bug in src/components/TopicView.jsx: the Quiz tab was always shown regardless of whether a topic had questions, which would've crashed on a flashcards-only topic like this one. Now it only shows when questions.length > 0 (same pattern the Flashcards tab already used).
- Ran npm run db:seed to load it into the database.